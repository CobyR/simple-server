# simple-server

This is a bare-bones web application written with golang.

It starts a webserver and responds on port 80, paths supported include:

* /
* /health
* and static files under /public

This is being used as part of a Falcon Hello World effort at MuleSoft.
